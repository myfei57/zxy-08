# KVGrid

KVGrid 是一个面向企业热点数据的分布式缓存与键值存储集群。写入按键定位分片，
分片归属在节点间可迁移，数据以文件形式持久化在本地磁盘；集群提供过期扫描、
内存淘汰、快照与恢复、配额控制以及操作审计等能力。

## 构建

```bash
docker build -t kvgrid .
```

## 运行

```bash
go run ./cmd/kvgrid -addr 127.0.0.1:7788 -data ./data -shards 8 -quota 1048576
```

启动后打开 http://127.0.0.1:7788 即可进入控制台，页面包含集群拓扑、键浏览、
快照管理和审计日志四个视图，JSON API 位于 /api 下。

## 数据目录

每个节点在数据目录下保存自己的写入日志、提交水位、删除墓碑与元数据；集群的
分片归属表、快照代际、审计日志和配额账本也在数据目录内持久化。数据目录可以直接
整体备份。
